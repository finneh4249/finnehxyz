---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="404 - NOPE, NOT HERE">
  <div class="signal-grid-bg" aria-hidden="true"></div>

  <main class="min-h-screen flex flex-col items-center justify-center px-6 py-24 relative z-10">

    <!-- Code label -->
    <p class="font-mono text-[10px] text-signal-orange/70 uppercase tracking-[0.5em] mb-6">Signal Error</p>

    <!-- Giant number -->
    <h1
      class="font-display font-black text-signal-white italic tracking-tighter leading-none mb-8"
      style="font-size: clamp(7rem, 25vw, 18rem); text-shadow: 12px 12px 0px var(--signal-orange);"
    >
      404
    </h1>

    <!-- Message -->
    <div class="border border-signal-white/10 bg-signal-charcoal/40 px-8 py-6 mb-10 max-w-md text-center">
      <p class="font-mono text-[11px] text-signal-white/40 uppercase tracking-[0.4em] mb-3">// Diagnostic</p>
      <p class="font-body text-signal-white/70 text-sm leading-relaxed">
        Route not resolved. The page you requested has no registered handler in this system.
      </p>
    </div>

    <!-- Status list -->
    <div class="font-mono text-[10px] text-signal-white/25 uppercase tracking-widest space-y-1.5 mb-10 text-left">
      <p><span class="text-signal-orange mr-2">[ERR]</span> Path not found in routing table</p>
      <p><span class="text-signal-amber mr-2">[WRN]</span> Request forwarded to fallback handler</p>
      <p><span class="text-signal-cyan mr-2">[INF]</span> No cached response available</p>
    </div>

    <!-- Actions -->
    <div class="flex flex-wrap gap-4 justify-center">
      <a href="/" class="signal-btn group">
        Home
        <span class="ml-3 group-hover:translate-x-1 transition-transform inline-block">→</span>
      </a>
      <button onclick="history.back()" class="signal-btn-ghost">
        Go back
      </button>
    </div>

  </main>
</BaseLayout>

<style>
  .signal-btn-ghost {
    display: inline-flex;
    align-items: center;
    font-family: 'Space Mono', monospace;
    font-weight: 900;
    font-size: 0.625rem;
    text-transform: uppercase;
    letter-spacing: 0.3em;
    padding: 1.25rem 2.5rem;
    border: 1px solid oklch(96% 0.005 258 / 0.2);
    color: oklch(96% 0.005 258 / 0.6);
    background: transparent;
    cursor: pointer;
    transition: border-color 0.2s, color 0.2s;
  }
  .signal-btn-ghost:hover {
    border-color: var(--signal-white);
    color: var(--signal-white);
  }
</style>